"""sea — self-evolving agent memory CLI (P0 prototype).

Local-first, Markdown-as-memory. Five-layer framework distilled into
three runtime memory levels:
  L1 scratch (working) / L2 logs (episodic) / L3 kb (semantic).
"""

__version__ = "0.1.0"
